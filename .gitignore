# Environment / secrets
.env
.env.local
.env.*.local
.env.development
.env.production
!.env.example

node_modules/
Duffel_working_APIs/

# Editor-local, not part of the product. The whole .claude/ directory is tooling
# permissions and local state — no secrets, but no value to a reader either.
.vscode/
.claude/

temp/
logs/
Help Docs/

# Internal working memory — deliberately NOT shipped in the public repo.
# Full of reversed decisions and self-corrections; would make a strong project read as
# uncertain. The pitch-facing README is the public artifact instead. See CLAUDE.md event step 0.
CLAUDE.md